apiVersion: v1
kind: Service
metadata:
  name: redis-dev-isma
spec:
  ports:
    - port: 6379
      name: redis
  clusterIP: None
  selector:
    app: redis-dev-isma
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-dev-isma
spec:
  selector:
    matchLabels:
      app: redis-dev-isma  # has to match .spec.template.metadata.labels
  serviceName: easy-rent-dev-redis
  replicas: 1
  template:
    metadata:
      labels:
        app: redis-dev-isma  # has to match .spec.selector.matchLabels
    spec:
      containers:
        - name: easy-rent-dev-redis
          image: redis:3.2-alpine
          imagePullPolicy: Always
          args: ["--requirepass", "$(REDIS_PASS)", "--appendonly", "yes", "--save", "900", "1", "--save", "30", "2"]
          ports:
            - containerPort: 6379
              name: redis
          env:
          - name: REDIS_PASS
            valueFrom:
              secretKeyRef:
                name: redis-secrets-dev
                key: REDIS_PASS
          volumeMounts:
            - name: redis-dev-volume
              mountPath: /data
  volumeClaimTemplates:
  - metadata:
      name: redis-dev-volume
    spec:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 10Gi